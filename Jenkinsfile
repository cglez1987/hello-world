pipeline {
    agent any
    stages{
        stage("Validate") {
            steps{
                echo "========Validating changes========"
            }
        }
        stage("Pull Request") { 
            steps{
                echo "====++ Testing pull request ++++===="
            }
        }
    }
}
